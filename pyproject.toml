[project]
name = "server-bootstrap"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "ansible-core>=2.19,<3",
  "pre-commit>=4.0,<5",
  "yamllint>=1.37,<2",
]
